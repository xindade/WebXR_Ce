# 2026-05-13 工作日志

## 结局引导流程修正

### 死亡/通关流程分离
- **死亡**: 0.3s黑闪 → 直接 `resetAndRestartAnimation()` (重置气球→DRIVE)，**跳过**中心引导/卡片
- **通关**: 隐去气球 → CENTER(绿色光圈, 1m半径或10s超时自动推进) → MAGICIAN_CENTER(魔术师飞到(0,1.5,-2), 4s) → SHOW_CARDS
- **3次失败**: 直接清理场景进入下一关

### 卡片系统修正
- SHOW_CARDS 阶段调用 `spawnChoiceCards(true)`（全部传说品质），使用原版左手射线+扳机选择
- 移除自定义 `spawnLaserEndCards`/`updateLaserEndCards` 函数
- 移除 `onLaserCleared()` 中的重复 `spawnChoiceCards` 调用
- `roundRect` 兼容性修复: 在 PICO 4 浏览器上添加 polyfill

### 问题3: 下一关前重复弹卡
已修复，`onLaserCleared` 不再调用 `spawnChoiceCards`，全部传说卡只在 SHOW_CARDS 阶段弹出一次。

### 卡片不可见根因
`hideShootingScene()` 将 `choiceCardGroup.visible = false`（保存原始值），但 `spawnChoiceCards` 把新卡片加到不可见的 Group 里。修复：SHOW_CARDS 中先 `choiceCardGroup.visible = true` 再生成卡片。

### 卡片面向修复
`updateChoiceCards` 每帧加 `card.lookAt(faceTarget)` 确保卡片始终朝向相机。

### 碰撞检测范围
移除 DRIVE 排除（现在全阶段检测，INTRO和结局除外），通关检测扩展到所有动画阶段。
