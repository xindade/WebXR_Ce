# 2026-05-08 工作日志

## 从 GitHub 同步代码
- 项目 `.git` 目录丢失，执行 `git init` + `remote add origin` + `fetch` + `reset origin/master` 恢复
- 当前最新提交: `959545e` fix: 修复移除如来神掌后 setVR 未导出导致的全局错误

## Bug 修复

### 1. 夜间气球贴图变淡（只剩颜色）
- **根因**: 夜间环境光(0.35)+太阳光(0.5)太弱，MeshStandardMaterial 的 map 贴图几乎不可见，只剩 emissive 发光色
- **方案**: 在 core.js 添加 `skyBrightness` 全局亮度因子（0~1），基于 ambientI+sunI 综合计算
- **修改**: createBalloon 时 emissiveIntensity = 0.15 + (1-skyBrightness)*0.6，夜间自动提高到 0.75
- **修改**: updateBalloons 中每帧 lerp 已有气球的 emissiveIntensity，实时跟随天空变化
- **涉及文件**: core.js（skyBrightness 计算）、game.js（创建+动态更新）

### 2. 如来神掌系统重新实现
- **根因**: 如来神掌在之前模块化重构时被完全移除，setVR 变为空函数
- **重新实现**: 完整的 IDLE→AIMING→SLAMMING 状态机
  - core.js: 添加 6 个 BUDDHA 常量 + 8 个 STATE 字段 + buddhaPalmGroup 容器
  - game.js: 恢复 setVR 注入 + attachBuddhaPalmToLeft + updateBuddhaPalm + releaseBuddhaPalm + resetBuddhaPalm
  - vr.js: 加载 如来神掌.glb 模型，左手腕 UI 显示神掌状态/冷却/瞄准倒计时
  - index.html: 动画循环调用 updateBuddhaPalm，sessionend 调用 resetBuddhaPalm
  - clearChoiceCards 中 waveNumber++ 后自动解锁神掌

### 3. BUDDHA_AIM_TIMEOUT 未导入错误
- **现象**: PICO 4 日志报 `BUDDHA_AIM_TIMEOUT is not defined`
- **根因**: vr.js 调试面板引用了该常量但 import 只加了 BUDDHA_COOLDOWN，漏了这个
- **修复**: vr.js import 补上 BUDDHA_AIM_TIMEOUT

### 4. 代码变更检查规则
- 跨文件改完后必须 grep 验证 import 完整性（已写入 MEMORY.md）

## 功能修改

### 1. 骑士气球必定带左右普通气球
- **修改**: game.js `spawnOneBalloon()` — 当 isKnight=true 时，额外在骑士左右各 1.2m 生成普通气球
- **效果**: 骑士出现时不再孤立，总有伴飞气球增加视觉层次

### 2. 选项卡跟随头显方向
- **修改**: game.js `spawnChoiceCards()` — 每张卡片记录 cardOffset（offsetX/offsetY/offsetZ）
- **新增**: `_repositionCard()` — 根据相机 forward/right + 偏移计算世界坐标
- **新增**: `updateChoiceCards(dt)` — 每帧用当前相机方向重算所有卡片位置+朝向
- **修改**: 刷新卡逻辑改为调用 `spawnChoiceCards()` 复用偏移系统，消除重复代码
- **修改**: index.html 动画循环添加 `game.updateChoiceCards(dt)` 调用
- **效果**: 选项卡始终在玩家正前方 0.75m，转头时卡片跟随移动

### 3. 修复选项卡刷新冷却功能
- **根因**: 重构 spawnChoiceCards 时，把 `choiceRefreshCount = 0` 放在了函数开头，导致刷新后费用重置
- **修复**: spawnChoiceCards 不再重置 choiceRefreshCount，改由 clearChoiceCards（新一轮）重置

### 4. 左手射线指向选项卡系统
- **修改**: vr.js `setupController()` — 左手柄添加直径0.3青色发光圆球（位置(0,-0.05,-0.15)）
- **修改**: game.js `updateChoiceCards()` — 每帧用 Raycaster 检测左手射线与卡片碰撞
  - 射线方向与子弹一致：左控制器 quaternion + -30°俯仰
  - 命中卡片：向玩家移近0.1 + 缩放1.2（平滑插值）
  - 未命中卡片：恢复原始缩放
- **修改**: game.js `checkLeftHandChoiceCardCollision()` — 从距离碰撞改为射线+扳机确认
  - 扳机上升沿检测（prevLeftTrigger），避免持续触发
- **新增 STATE 字段**: leftRaySphere, highlightedCardIndex
- **涉及文件**: core.js, vr.js, game.js

### 5. 参数标注与常量提取
- 将选项卡、如来神掌、左手射线球的所有硬编码魔术数字提取到 core.js 常量
- **选项卡常量**: CARD_WIDTH/HEIGHT, REFRESH_HEIGHT, CARD_SPACING, REFRESH_OFFSET_Y, CARD_Y_OFFSET, HIGHLIGHT_PULL/SCALE/LERP
- **射线球常量**: RAY_SPHERE_RADIUS/POS/COLOR, RAY_PITCH_ANGLE, RAY_CAST_DISTANCE
- **如来神掌常量**: HAND_SCALE/POS/ROT_X, PREVIEW_SCALE/POS/OPACITY, FALL_START/END_SCALE, FALL_HEIGHT/FORWARD, IMPACT_CLEANUP
- 所有引用点已替换为常量引用，import 链完整验证通过

## 如来神掌重构（2026-05-08 第二次修改）

### 1. 修复如来神掌角度（竖直 → 平放）
- **根因**: `BUDDHA_HAND_ROT_X` 为 `Math.PI/4`（45°），手掌呈竖直状态
- **修复**: 改为 `-Math.PI/2`，手掌平放
- `releaseBuddhaPalm()` 中新增 `palm.rotation.x = BUDDHA_HAND_ROT_X`

### 2. 去掉预览，去掉 AIMING 状态，释放无倒计时
- **修改**: `core.js` 删除 `BUDDHA_AIM_TIMEOUT`、`BUDDHA_PREVIEW_SCALE/POS/OPACITY`
- **修改**: `game.js` `updateBuddhaPalm()` — IDLE 状态检测到 grip 上升沿直接调用 `releaseBuddhaPalm()`，不再进入 AIMING
- **修改**: 删除预览生成代码和 `buddhaPalmPreview` 相关清理
- **修改**: `resetBuddhaPalm()` 去掉预览清理和 `buddhaAimTimer` 重置

### 3. 杀伤范围直接拉满
- **修改**: `BUDDHA_KILL_RADIUS` 从 10 改为 50（直接拉满）

### 4. 开始界面新增关卡选择按钮
- **修改**: `index.html` `#vr-entry` 改为 flex 布局，右侧新增 `#level-container`（3个选关按钮）
- **逻辑**: 选关后设置 `gold=1000`, `atk=100`，高亮当前选择
- **修改**: `sessionstart` 中若 `selectedLevel > 0` 则应用属性并设置 `waveNumber = selectedLevel - 1`

### 5. import 链修复
- **修改**: `game.js` import 删除 `BUDDHA_AIM_TIMEOUT`、`BUDDHA_PREVIEW_*`，修复重复导入行
- **修改**: `vr.js` import 删除 `BUDDHA_AIM_TIMEOUT`
- **涉及文件**: core.js, game.js, vr.js, index.html

## 第三次修改（2026-05-08）

### 1. 添加左手可见射线 + 参数标注
- **修改**: `vr.js` `setupController()` — 左手射线球后添加可见 `THREE.Line`（青色半透明，从球体沿射线方向延伸3m）
- **修改**: `core.js` — 为 `RAY_SPHERE_POS`（x/y/z 三轴分别标注释）和 `RAY_PITCH_ANGLE`（解释-30°作用）添加详细注释

### 2. 选关后如来神掌直接解锁
- **根因**: `clearChoiceCards()` 中神掌解锁条件为 `waveNumber >= 1`，选关时 `waveNumber=1` 但模型未加载，需等一轮后才触发
- **修改**: 新增 `STATE.buddhaPalmUnlocked` 标记，见下文

**修改方案**:
1. `core.js` STATE — 删除 `buddhaPalmPreview` 和 `buddhaAimTimer`（已废弃），新增 `buddhaPalmUnlocked: false`
2. `vr.js` — 导入 `attachBuddhaPalmToLeft`，模型加载回调中检查 `STATE.buddhaPalmUnlocked` 并立即调用
3. `index.html` — `sessionstart` 中选关时设置 `STATE.buddhaPalmUnlocked = true`；`sessionend` 重置为 false 并清 `selectedLevel`
- **效果**: 选关卡 2/3 后，如来神掌模型加载完成即刻解锁，无需先打一轮

### 3. 修复 RAY_PITCH_ANGLE 未导入 + 调试面板引用废弃常量
- **根因**: 新增可见射线时 vr.js 使用了 `RAY_PITCH_ANGLE` 但未 import；调试面板仍引用已删除的 `BUDDHA_AIM_TIMEOUT` 和 `STATE.buddhaAimTimer`
- **修改**: `vr.js` import 添加 `RAY_PITCH_ANGLE`；删除 `updateLeftDebugPanel` 中的 AIMING 分支代码
- **验证**: 对所有已删除常量和新常量做 grep 遍历，确认无遗漏引用

## 第四次修改（2026-05-08）

### 1. 修复选关如来神掌仍未解锁（时序问题）
- **根因**: 模型在模块初始化时已加载完成，此时 `buddhaPalmUnlocked` 为 false，加载回调中的检查`不通过
- **修复**: `sessionstart` 中设 `buddhaPalmUnlocked=true` 后，同步检查 `STATE.buddhaPalmModel && STATE.leftGrip`，若已就绪则直接调用 `game.attachBuddhaPalmToLeft()`
- **效果**: 选关后神掌立即解锁，无需等待任何异步逻辑

### 2. 刷新卡片射线触碰不灵敏
- **根因**: `updateChoiceCards` 中高亮仅检查 `card.userData.isChoiceCard`，刷新卡被排除，无视觉反馈
- **修复**: 改为 `isChoiceCard || isRefreshCard`，刷新卡被射线指向时同样有靠近+放大效果

### 3. 气球船→鲲鹏 + 标注参数
- **修改**: `vr.js` 模型加载路径从 `Model/气球船.glb` → `Model/鲲鹏.glb`
- **修改**: `core.js` 为 `SHIP_SCALE`、`SHIP_POS`、`SHIP_ROT` 添加三轴详细注释
- **涉及文件**: core.js, game.js, vr.js, index.html

